package enums;

/**
 * Enum which houses the different states of the game.
 * 
 * @author Robert Wilson
 *
 */
public enum GameState {
	TITLESTATE,
	PLAYSTATE, 
	PAUSESTATE,
	DIALOGUESTATE,
	CHARACTERSTATE,
	DEADSTATE;
}
